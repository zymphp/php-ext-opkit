# OpKit 待办事项 (TODOs)

## 核心特性
- [x] **常量支持**: OpKit 现在支持用户定义的持久化常量（包括 `define()` 和类常量）。
- [x] **多版本兼容性**: OpKit 现在可以同时支持 PHP 8.2 和 8.3 版本。正在计划适配 PHP 8.4 和 8.5 版本。
- [ ] **JIT 共存研究**: 研究使 OpKit 与 Zend OPcache 的 JIT 兼容的可能性，同时保持性能。

## 工具与易用性
- [x] **phpc 配置支持**: 允许通过配置文件（如 `opkit.json`）管理编译参数。
- [x] **增量编译**: 基于源码修改时间、System ID 和 Magic 值的智能增量编译。
- [x] **交互式信息查看**: 增强 `phpc -i` 以支持列出详细的方法签名和类属性。
- [x] **Composer 支持**: 将phpc脚本挪到opkit的bin目录下，当前项目可作为 Composer 依赖，提供自动安装
  `phpc` 脚本到 `vendor/bin` 目录下，同时还提供对opkit扩展的编译安装能力（通过 Composer 插件实现）。
- [ ] **Windows 支持**: 验证并修复 Windows 系统上的兼容性问题（例如路径处理和文件锁定）。

## 性能与稳定性
- [x] **内存池优化**: 改进了持久化内存的布局，以减少碎片并提高 CPU 缓存命中率（实现了分区布局：Metadata、Code、Data、Misc）。
- [x] **并发安全性**: 实现了原子写入（临时文件 + 重命名）和文件锁，确保在多进程环境下文件不会损坏。
- [x] **性能分析 (Profiling)**: `phpc` 提供详细的单个文件编译时间和总耗时统计。
- [ ] **全面基准测试套件**: 开发一套标准基准测试，用于评估和展示不同类型应用程序的性能提升。

## 错误处理
- [x] **友好错误提示**: 在编译失败时捕获并显示具体的错误原因和异常信息，支持跳过错误文件并继续批量编译。
- [x] **静态分析支持**: 增强了 `opkit_get_info` 以导出详细的类、函数和常量元数据（包含类型签名、可见性和静态标志），并实现了 `phpc --stubs` 工具用于生成 PHP 定义存根（Stubs），同时新增了 `phpc analyze` 指令用于汇总分析和冲突检测。

## 高级 Phar 支持
- [x] **归档优化**: 在 `phpc` 中支持 Phar 压缩（GZip, BZip2）和数字签名。
